<header id="header" class="header-section">
  <h1 class="portfolio-title">
    Steven Mendez <span class="gradient-text">Software Developer</span>
  </h1>
  <p class="text">Turning challenges into solutions.💡</p>
</header>
<style>
  :root {
    --header-padding-small: 20px;
    --header-padding-large: 32px;
    --mask-image: radial-gradient(rgba(0, 0, 0, 0.45), transparent 70%);
  }

  .header-section {
    /* Apply flexbox layout to center the content */
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding-top: var(--header-padding-small);
    min-width: 100%;
    height: 100vh;
    box-sizing: border-box;
  }

  .portfolio-title {
    margin-bottom: 0.75rem;
    padding-left: 0.5rem;
    padding-right: 0.5rem;
    text-align: left;
    font-size: 8rem;
    font-weight: 600;
    line-height: 1;
    margin-bottom: 3rem;
  }

  .gradient-text {
    background: -webkit-linear-gradient(45deg, #40cfff 0%, #fdf48e 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-size: 0.5em;
    display: block;
  }

  .text {
    padding-left: 0.5rem;
    padding-right: 0.5rem;
    text-align: left;
    font-size: 1.25rem;
  }

  .header-section::before {
    content: '';
    position: absolute;
    top: 0%;
    bottom: 10%;
    width: 150%;
    left: -25%;
    background: var(--color-background-gradient);
    -webkit-mask-image: var(--mask-image);
    mask-image: var(--mask-image);
    z-index: -1;
  }

  @media only screen and (max-width: 1280px) {
    .portfolio-title {
      font-size: 5rem;
      margin-bottom: 1.5rem;
    }
    .gradient-text {
      font-size: 0.6em;
    }

    .text {
      max-width: 890px;
      font-size: 1rem;
    }
  }

  @media only screen and (max-width: 890px) {
    .portfolio-title {
      font-size: 4rem;
    }

    .text {
      max-width: 717px;
    }
  }

  @media only screen and (max-width: 720px) {
    .portfolio-title {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    .text {
      max-width: 545px;
    }
  }

  @media only screen and (max-width: 545px) {
    .portfolio-title {
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .text {
      max-width: 360px;
    }
  }

  @media only screen and (max-width: 360px) {
    .portfolio-title {
      font-size: 1.4rem;
      margin-bottom: 0.5rem;
    }

    .text {
      max-width: 266px;
      font-size: 0.5rem;
    }
  }
</style>
